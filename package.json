{
  "name": "wiki-mintlify-template",
  "private": true,
  "scripts": {
    "dev": "mint dev",
    "validate": "mint validate",
    "export": "mint export --output dist",
    "sync": "bash scripts/sync-docs.sh"
  },
  "devDependencies": {
    "mintlify": "^4.2.639"
  }
}
